{
  "name": "discord-agent-worker",
  "version": "0.1.0",
  "description": "GitHub Actions worker for Discord AI bot — fetches YouTube transcripts (bypassing Cloudflare IP blocks) and processes via Gemini",
  "type": "module",
  "private": true,
  "scripts": {
    "summarize": "node scripts/summarize-youtube.js"
  },
  "dependencies": {},
  "engines": {
    "node": ">=20"
  }
}
